#!/usr/bin/perl
# SSH Agent wrapper
use strict; use warnings;

# load key if it isn't already
if (system "ssh-add -l > /dev/null") {
    system "ssh-add ~/.ssh/id_rsa"
}

# run 
exec "/usr/bin/ssh @ARGV";
